namespace FlyerFlipper.Core.Imaging;

public enum ImagePixelFormat
{
    /// <summary>
    /// 8 bits per channel, byte order B, G, R, A, with color channels premultiplied by alpha.
    /// </summary>
    Bgra8888Premultiplied,
}
